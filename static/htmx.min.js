/*
 * htmx.min.js — placeholder
 *
 * Exécutez `make deps` pour télécharger automatiquement HTMX v2.
 *
 * Ou manuellement :
 *   curl -sL https://unpkg.com/htmx.org@2.0.4/dist/htmx.min.js -o static/htmx.min.js
 *
 * L'application fonctionne SANS HTMX (formulaires HTML natifs).
 * HTMX active la mise à jour rapide du statut sans rechargement complet.
 */
